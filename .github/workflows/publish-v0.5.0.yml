name: v0.5.0 published

# v0.5.0 was published successfully. This inert file remains only on the
# temporary release branch and is not part of main.
on:
  workflow_dispatch:

jobs:
  notice:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo 'v0.5.0 has already been published.'
